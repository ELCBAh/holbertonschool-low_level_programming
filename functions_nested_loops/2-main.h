#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - initializes print alphabet
 * Description: prints alphabet x10 times
 * Return: 0
 */
void print_alphabet(void)
	int count =;
	for(count = 0; count < 11; count++)
	{
		print_alphabet();
	}
